<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
      color: #333;
    }
    header {
      background-color: #2c3e50;
      color: white;
      padding: 1rem 0;
      text-align: center;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }
    .breadcrumb {
      margin: 1rem 0;
      color: #666;
    }
    .breadcrumb a {
      color: #3498db;
      text-decoration: none;
    }
    .portfolio-detail {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 30px;
      margin: 2rem 0;
    }
    .portfolio-image {
      border-radius: 5px;
      overflow: hidden;
    }
    .portfolio-image img {
      width: 100%;
      height: auto;
      display: block;
    }
    .portfolio-info h1 {
      margin-top: 0;
      color: #2c3e50;
    }
    .testimonial {
      background-color: #f4f4f4;
      padding: 1.5rem;
      border-radius: 5px;
      margin-top: 2rem;
      font-style: italic;
    }
    .testimonial-author {
      font-weight: bold;
      font-style: normal;
      margin-top: 1rem;
    }
    .section {
      margin-bottom: 2rem;
    }
    footer {
      background-color: #2c3e50;
      color: white;
      text-align: center;
      padding: 1rem 0;
      margin-top: 2rem;
    }
    .btn {
      display: inline-block;
      background: #3498db;
      color: white;
      padding: 0.8rem 1.5rem;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      margin-top: 1rem;
    }
    @media (max-width: 768px) {
      .portfolio-detail {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="container">
      <h1>GraphicShop</h1>
    </div>
  </header>

  <div class="container">
    <div class="breadcrumb">
      <a href="/">Home</a> / <a href="/portfolio">Portfolio</a> / <%= item.title %>
    </div>

    <div class="portfolio-detail">
      <div class="portfolio-image">
        <img src="<%= item.imageUrl %>" alt="<%= item.title %>">
      </div>

      <div class="portfolio-info">
        <h1><%= item.title %></h1>
        <p><strong>Client:</strong> <%= item.clientName %></p>
        <p><strong>Category:</strong> <%= item.category %></p>

        <div class="section">
          <h2>Project Description</h2>
          <p><%= item.description %></p>
        </div>

        <div class="section">
          <h2>Design Process</h2>
          <p><%= item.process %></p>
        </div>

        <div class="section">
          <h2>Results</h2>
          <p><%= item.results %></p>
        </div>

        <% if (item.testimonial) { %>
          <div class="testimonial">
            "<%= item.testimonial.content %>"
            <div class="testimonial-author">
              - <%= item.testimonial.clientName %><% if (item.testimonial.clientCompany) { %>, <%= item.testimonial.clientCompany %><% } %>
            </div>
          </div>
        <% } %>

        <a href="/contact" class="btn">Request Similar Design</a>
      </div>
    </div>
  </div>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> GraphicShop. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
